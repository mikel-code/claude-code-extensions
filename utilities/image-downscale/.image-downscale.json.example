{
  "_comment": "Example configuration for image-downscale skill",
  "_usage": "Copy this file to .image-downscale.json in your target directory and customize",

  "scan_paths": [
    "07/Organized/Images",
    "Attachments",
    "Resources"
  ],
  "_scan_paths_note": "Array of subdirectories to scan (relative paths). If empty or missing, scans entire directory",

  "max_width": 1200,
  "_max_width_note": "Maximum width in pixels for downscaled images. CLI --max-width overrides this",

  "size_threshold_kb": 500,
  "_size_threshold_kb_note": "Only process images larger than this (in KB)",

  "dimension_threshold_px": 1200,
  "_dimension_threshold_px_note": "Only process images wider or taller than this (in pixels)"
}
